# Next

## In Flight

- MVP-0 multi-camera layout export is available at `scripts/render_multicamera_layout.py`. It consumes the original SMART validation `HeteroData` / `Batch` path through a normal config/checkpoint, runs model inference, and renders six synthetic camera-view layout PNG sequences plus `manifest.json` for downstream video-conditioning experiments. Road/map centerlines are disabled by default; use `--draw-map-polylines` only for geometry debugging.
- Layout export currently uses an approximate nuScenes-like six-camera pinhole rig (`CAM_FRONT`, `CAM_FRONT_LEFT`, `CAM_FRONT_RIGHT`, `CAM_BACK`, `CAM_BACK_LEFT`, `CAM_BACK_RIGHT`). Treat the output as geometry/layout conditioning, not calibrated real-camera rendering.
- `smart_hybrid_diffusion` is available as an additive predictor. It is a composition-based `pl.LightningModule`, not a subclass of `SMART`, `SMARTDiffusion`, `SMARTAutoregressiveDiffusion`, or `SMARTCausalDiffusion`; it keeps original SMART `HeteroData` / `Batch` inputs and uses `diffusion.hybrid_objective: closed_loop_frontier_v1`.
- Hybrid configs are available at `configs/train/train_scalable_hybrid_diffusion_local.yaml`, `configs/train/train_scalable_hybrid_diffusion_1000.yaml`, and `configs/validation/validation_scalable_hybrid_diffusion.yaml`. The matched 1000-step config is aligned with the existing local comparison configs and adds bidirectional commit-speed calibration with `commit_min_speed_ratio: 0.75`, `commit_max_speed_ratio: 1.25`, and `commit_speed_reference_decay: 1.0`.
- `smart_elf` is now a standalone receding-horizon additive predictor. It composes the SMART map/history encoder, samples four-token official-ELF-style embedding-flow windows, commits one token, rolls token and frame history, and re-encodes before the next window; it does not inherit `SMARTAutoregressiveDiffusion`, `SMARTCausalDiffusion`, `SMARTDiffusion`, or the diffusion decoder. Window attention is chunk-causal: agents in the same chunk can attend to each other, and earlier chunk queries cannot attend to later chunks.
- ELF configs use `diffusion.elf_objective: embedded_language_flow_v1`, `elf_sequence_tokens: 16`, `elf_window_tokens: 4`, `elf_commit_tokens: 1`, `elf_decoder_prob: 0.25`, `target_category_only: false`, and `supervision_mode: all_agents`; they no longer declare `ar_objective`, `causal_objective`, retokenization, or safety-guidance fields. ELF passes its generated-agent mask into SMART history-context map attention so all current-valid non-background generated agents receive direct map2agent context, then uses `elf_map_commit_score_weight`, `elf_map_commit_loss_weight`, and `elf_geometry_energy_weight` to make commit token selection map-grounded without replacing ELF with SMART's original next-token head.
- Local ELF training config is available at `configs/train/train_scalable_elf_3epoch_local.yaml`; launch it with `--save_ckpt_path checkpoints/elf_receding_3epoch`.
- The old `checkpoints/elf_ar_1000/last.ckpt`, previous standalone full-window ELF checkpoints, category-only ELF checkpoints, overwrite-only-history ELF outputs, and pre-map-conditioned receding ELF checkpoints are stale for quality comparisons; retrain before judging loss, accuracy, map compliance, or visualization quality.
- A CPU validation-sample smoke for the receding ELF path on 2026-06-16 produced finite full rollout output with `pred_traj=(58, 80, 2)`, `next_token_idx=(58, 16)`, and `valid_frames=3777`.
- `smart_ar_diffusion` now has an opt-in causal-frontier v2 objective. Use `configs/train/train_scalable_ar_diffusion_baseline_1000.yaml` for the old AR baseline and `configs/train/train_scalable_ar_diffusion_frontier_local.yaml` for the new AR frontier model.
- Matched 1000-step comparison configs are available for `ar_baseline`, `ar_frontier`, `causal_diffusion`, `causal_flow_matching`, `smart_elf`, `hybrid_diffusion`, `ar_action_chunk`, and `continuous_action_diffusion`; all use `/home/anthony/SimAgentJEPA/data/waymo/training_subset_10pct` for training and `/home/anthony/SimAgentJEPA/data/waymo/validation` for validation. These configs use `strategy: auto`, validate at step 1000, save a step-1000 checkpoint, and keep `last.ckpt` so interrupted mid-epoch runs still leave a usable comparison artifact.
- Local 1000-step AR baseline training completed under `checkpoints/ar_baseline_1000/last.ckpt`; final logged validation metrics were `val_minADE=2.920`, `val_minFDE=8.670`, `val_ar_window_loss=3.440`, and `val_ar_window_mask_acc=0.290`. Keep generated checkpoints, Lightning logs, and `outputs/` out of commits.
- Local 1000-step AR causal-frontier training completed under `checkpoints/ar_frontier_1000/last.ckpt`; final logged validation metrics were `val_minADE=4.520`, `val_minFDE=12.70`, `val_ar_window_loss=4.020`, and `val_ar_window_mask_acc=0.187`. Keep generated checkpoints, Lightning logs, and `outputs/` out of commits.
- Local 1000-step causal diffusion training completed under `checkpoints/causal_diffusion_1000/last.ckpt`; final logged validation metrics were `val_minADE=3.020`, `val_minFDE=9.990`, `val_ar_window_loss=4.040`, `val_ar_window_mask_acc=0.249`, and `val_rollout_score=24.10`. Keep generated checkpoints, Lightning logs, and `outputs/` out of commits.
- Local 1000-step causal flow-matching training completed under `checkpoints/causal_flow_matching_1000/last.ckpt`; final logged validation metrics were `val_minADE=2.450`, `val_minFDE=8.410`, `val_ar_window_loss=0.000464`, `val_ar_window_mask_acc=1.000`, and `val_rollout_score=19.40`. Keep generated checkpoints, Lightning logs, and `outputs/` out of commits.
- Local 1000-step ACT-style action-chunk training completed under `checkpoints/ar_action_chunk_1000/last.ckpt`; final logged validation metrics were `val_minADE=3.2032`, `val_minFDE=11.4179`, `val_ar_window_loss=1.3118`, and `val_ar_window_mask_acc=0.1866`. That checkpoint predates dense SMART CE replay and overlap shift-consistency, so treat it as stale for quality comparisons. Validation and step visualizations are under `outputs/val_ar_action_chunk_1000/` and `outputs/step_ar_action_chunk_1000/`. Keep generated checkpoints, Lightning logs, and `outputs/` out of commits.
- `smart_ar_diffusion` now has an AR-first rerank variant that keeps `ar_objective: maskgit`, 4-token prediction, 1-token commit, and tail proposal carry, while adding causal temporal token attention, same-scene/same-chunk spatial token attention, terminal PAD windows, commit-primary chunk weights `[1.0, 0.3, 0.1, 0.05]`, cadf_lite single-window training, deterministic chunk0 anchor cycling, local next-token CE from the same window encoder context, all-mask/carry-over proposal initialization, periodic full-sequence SMART CE replay, sampling-time safe-speed commit reranking, and config-gated SMART-style map-token noise/history-context dropout. Train configs set `sampling_guidance.enabled: false`; use the validation config's safe-speed guidance as an inference ablation. Use `configs/train/train_scalable_ar_diffusion_rerank.yaml` for server training, `configs/train/train_scalable_ar_diffusion_rerank_1000.yaml` for the matched local 1000-step ablation, and `configs/validation/validation_scalable_ar_diffusion_rerank.yaml` for validation.
- `smart_action_chunk_diffusion` is available as an ACT/ALOHA-style AR ablation. It predicts four-token windows, commits one token, disables tail proposal conditioning, restores interval dense full-sequence SMART CE replay, adds action-chunk shift-consistency over adjacent overlapping windows, and uses confidence-weighted temporal voting over overlapping chunk predictions before commit. Use `configs/train/train_scalable_ar_action_chunk.yaml` for server training, `configs/train/train_scalable_ar_action_chunk_1000.yaml` for local 1000-step runs, and `configs/validation/validation_scalable_ar_action_chunk.yaml` for standalone validation.
- `smart_continuous_action_diffusion` is available as the diffusion-policy branch. It keeps SMART map/history context and AR receding-horizon commit semantics, denoises four-token / twenty-frame local continuous action chunks, ensembles overlapping predictions in continuous world-trajectory space, commits only chunk 0, and retokenizes the committed chunk only for SMART history/interface compatibility. Use `configs/train/train_scalable_continuous_action_diffusion.yaml` for server training, `configs/train/train_scalable_continuous_action_diffusion_1000.yaml` for local 1000-step runs, and `configs/validation/validation_scalable_continuous_action_diffusion.yaml` for standalone validation.
- `smart_discrete_diffusion_policy` is available as the pure discrete diffusion-policy branch. It predicts four-token SMART-token windows, trains only the diffusion denoiser with batched multi-anchor chunk-weighted x0 CE plus optional overlap KL, logs per-horizon x0 CE/accuracy, disables original SMART NTP CE/prior fusion, proposal memory, temporal ensembling, sampling guidance, candidate energy rerank, and self-conditioning, commits only chunk 0, and discards chunks 1-3. Batched anchor construction now prepares the merged anchor batch after raw per-anchor view batching, avoiding missing `pt_valid_mask` and malformed dynamic map-token edges in local training; the batched training step skips redundant raw-batch preparation and computes shifted overlap KL with vectorized tensor matching. Agent token embedding and SMARTDiffusion physical token embedding are now dtype-safe for AMP `16-mixed` token embeddings. Use `configs/train/train_scalable_discrete_diffusion_policy.yaml` for full server training, `configs/train/train_scalable_discrete_diffusion_policy_2000.yaml` only for a tiny local `data/valid_demo` functional smoke, and `configs/validation/validation_scalable_discrete_diffusion_policy.yaml` for standalone validation.
- Local 2000-step discrete-policy smoke completed at `checkpoints/discrete_diffusion_policy_2000/last.ckpt` with TensorBoard `version_98` final smoke metrics `val_minADE=4.3555`, `val_minFDE=10.0936`, `val_ar_window_loss=3.0050`, and `val_ar_window_mask_acc=0.3556`. Step PNGs are under `outputs/step_discrete_diffusion_policy_2000/smart_discrete_diffusion_policy/step_002000/`; validation PNGs are under `outputs/val_discrete_diffusion_policy_2000/smart_discrete_diffusion_policy/epoch_182/`. Treat this as a functional smoke only, not quality evidence.
- Flow-matching objective diagnostics were fixed after the 1000-step run: old `val_ar_window_loss` and `val_ar_window_mask_acc` are not comparable to new runs because the old MSE was diluted by token vocabulary dimension and the old accuracy was computed from teacher-forced `flow_state`. Retrain before using flow-matching window loss/acc as quality evidence.
- `scripts/compare_motion_models.py` has produced the matched 1000-step comparison under `outputs/model_comparison_1000/` with records, summary, manifest, and 32 validation PNGs across four models and indices 0-7. The 8-scene ADE/FDE summary is `causal_flow` `5.4096/11.4135`, `ar_baseline` `5.8834/12.1168`, `causal_diffusion` `6.4491/13.6338`, and `ar_frontier` `7.9828/15.6503`, all with coverage `1.0`.
- Causal flow matching is now available as additive predictor `smart_causal_flow_matching`. Use `configs/train/train_scalable_causal_flow_matching_local.yaml` for local smoke/training, `configs/train/train_scalable_causal_flow_matching.yaml` for server training, and `configs/validation/validation_scalable_causal_flow_matching.yaml` for validation.
- Current flow-matching evidence includes the trained 1000-step local checkpoint and the matched 8-scene comparison above. Treat this as a local smoke-scale quality check, not a paper-scale evaluation; use a larger fixed validation slice before drawing final quality conclusions.
- Causal training curriculum changed on 2026-06-13: model-rollout states are now eligible from epoch 0 at `closed_loop_batch_ratio_max` probability, while perturb states still start at epoch 4. New runs should compare against older three-phase checkpoints with this schedule change called out explicitly.
- Latest epoch-4 inference-guidance sweep: use `outputs/causal_guidance_epoch04_setting_sweep_core/` and `outputs/causal_guidance_epoch04_setting_sweep_speed_floor/` for setting comparisons. The best tested setting is `guidance.mode=safe`, `commit_min_speed_ratio=0.75`, and `commit_speed_reference_decay=1.0`: ADE/FDE `2.2930/5.2324`, pred speed `1.2919`, moving-speed ratio `0.7485`, p50 ratio `0.7005`. Default safe was ADE/FDE `2.5538/5.7098`, pred speed `1.0400`, moving-speed ratio `0.6118`, p50 `0.5651`.
- Do not treat default safe energy as the sole cause of epoch-4 slow predictions. `safe_top1_no_energy` still had low moving-speed ratio `0.6199` while improving ADE/FDE over stochastic `none`, so the top-ranked token logits are already conservative; `none` looks faster mostly because multinomial sampling picks more aggressive but less accurate tokens.
- After relaunching with epoch-0 rollout training, validate the `commit_min_speed_ratio=0.75`, `commit_speed_reference_decay=1.0` inference setting on real validation scenes beyond the 11 demo scenes, then regenerate whole-scene visuals for that setting versus default safe and none.
- Use `outputs/causal_guidance_epoch04_whole_scene_none_safe/compare/` for whole-scene visual inspection of the current epoch-4 checkpoint. It contains all 11 `data/valid_demo` scenes rendered as side-by-side `none` versus `safe`, with separate full-scene single-mode PNGs under `per_mode/`.
- The 4-token coordinate path now has real-token regression coverage: `tests.test_smart_diffusion_smart_parity.SMARTDiffusionSMARTParityTest.test_real_token_four_chunk_geometry_and_heading_are_query_relative` loads `cluster_frame_5_2048.pkl`, checks `veh/ped/cyc`, and verifies positions plus headings across multiple start headings. Do not treat the current slow/ADE issue as a simple chunk 1-3 coordinate-frame bug unless a real-scene probe contradicts this.
- The actual 4-token geometry caveat is masked-tail conditioning: with only chunk 0 known, chunks 1-3 share the latest known fallback pose and heading until proposal geometry is available. This differs from official SMART's strict one-token recurrent query/feature update and remains a plausible source of weaker tail map/A2A conditioning.
- Latest speed diagnostic: the actual epoch-4 file in this workspace is `/mnt/d/casual_v2_epoch=04.ckpt` (`casual`, not `causal` in the filename). It loads as `epoch=4`, `global_step=21745`, with `missing=0`, `unexpected=0`. The exact `/mnt/d/causal_v2_epoch=04.ckpt` path is not visible here.
- Use `outputs/causal_speed_diag_epoch04_none_safe/summary.csv` and `records.csv` as the current matched `none`/`safe` speed baseline. On all 11 `data/valid_demo` scenes, `none` mean pred speed is `1.9128` vs GT `1.7695`, moving-speed ratio `0.7944`, p50 `0.7185`, pair-weighted ratio `0.8236`; `safe` is `1.0400`, ratio `0.6118`, p50 `0.5651`, pair-weighted ratio `0.5982`.
- Revise the speed-collapse diagnosis for epoch 4: raw `none` is not the severe low-speed collapse seen at epoch 2, but default `safe` slows predictions substantially while improving ADE/FDE (`3.7364/8.9915 -> 2.5538/5.7098`). The current best inference-only tuning is a stronger persistent commit-speed floor (`commit_min_speed_ratio=0.75`, `commit_speed_reference_decay=1.0`), but raw top-1 logits still remain conservative.
- When diagnosing causal v2 checkpoints, use the updated `scripts/smoke_causal_guidance_modes.py` speed fields (`pred_speed`, `gt_speed`, `moving_speed_ratio`, `moving_speed_ratio_p10/p50/p90`) alongside ADE/FDE. A low moving-speed ratio is now the primary signal for the conservative/near-static collapse.
- The current hybrid commit path keeps causal diffusion proposals/editing but adds chunk-0 speed-reference reranking. On `data/valid_demo`, it improves `safe` speed ratios but does not fully remove low-speed bias; next tuning should compare `commit_speed_energy_weight`, `commit_min_speed_ratio`, and `commit_speed_reference_decay` on real validation scenes, not only demo scenes.
- Do not interpret old smoke FDE artifacts from non-contiguous valid masks as map exits; FDE now uses the last actual valid frame. Re-run old smoke/visual results before comparing FDE numbers.
- Re-run old ego-risk smoke/visual outputs before comparing `ego_risk_min_ttc` or `token_change_rate_vs_gt`; no-finite TTC now stays infinite and token-change rate now uses only valid token slots as the denominator.
- The local workspace does not currently have the causal v2 epoch-1 checkpoint referenced by `/mnt/d/causal_v2_epoch=02.ckpt`; `/mnt/d/90113f8_epoch=01.ckpt` and `/mnt/d/90113f8_v2_epoch=01.ckpt` are `smart_ar_diffusion`, not causal v2. Copy `/raid/haoq_lab/wangshijie/SMART/checkpoints/causal_v2/epoch=01.ckpt` locally before epoch-1/epoch-2 causal comparison.
- Validate generic ego-risk guidance on checkpoint scenes with `seed,none,safe,ego_stress,ego_edit`; focus on ego-only low-TTC, near-miss, hard-collision/offroad, edit distance, and non-target preservation. Do not optimize the main study around predefined target-event success.
- Use `outputs/causal_ego_risk_guidance/` as the first generic low-TTC smoke sweep over the 11 demo scenes. It is useful for debugging scoring and aggregation, not yet for paper claims.
- Use `outputs/causal_ego_risk_guidance_visual/idx_00010_1ce0b4bbd35a6ad1_guidance_modes.png` as the current generic ego-risk visual smoke. It shows the controlled target and ego/SDC without cut-in-specific markers, but the visual effect is still subtle.
- Use `docs/ego_risk_waymo_validation_candidates.md` as the current real-validation candidate list. The best visual examples from the first 100 validation scenes are indices 72, 87, 31, 17, 41, and 33.
- Treat `outputs/causal_ego_guidance_cut_in_success/` and `outputs/causal_ego_guidance_cut_in_scene9_agent4_marked/` as legacy cut-in ablation artifacts only. They are no longer the primary direction.
- Extend ego-risk sweeps beyond the first 100 validation scenes and improve target-agent/window selection around ego interactions because the current evidence is still a screen, not paper-scale evaluation.
- Launch causal v2 from scratch; do not resume `/mnt/d/causal_epoch=00.ckpt`, because the objective, state conditioning, proposal path, and decoder inputs changed.
- Use the frozen 10,000-scene perturbed P99 thresholds already written to all causal configs.
- Compare causal v2 against original SMART, the old causal checkpoint, and the current AR diffusion checkpoint using identical validation scenes.
- Compare `val_rollout_score`, 2/4/6/8-second ADE/FDE, late ADE, lane/collision energies, map violations, and official SMART metrics against original SMART and the current AR diffusion checkpoint.
- Inspect `outputs/ar_map_rollout_query_debug_6c1658d_epoch00/` first for `/mnt/d/6c1658d_epoch=00.ckpt`; verify raw q overlap, proposal-refreshed q expansion, sampled q placement, rolled agents/history, and raw/proposal/sampled map-edge coverage before tuning self-conditioning/corruption schedules.
- After the history-context map mask alignment, rerun AR diffusion validation/visualizations with the checkpoint-matched config and compare agent context/map-edge coverage for category-3 targets versus non-target generation agents.
- Inspect `next_token_idx` predicted-token speed versus `next_token_idx_gt` for current-valid straight vehicles, split by `category == 3`, non-target generation agents, commit/proposal mode, visible-token corruption on/off, and causal-disabled/multiplier/fixed schedule variants.
- Continue comparing `smart_ar_diffusion` against full-horizon `smart_diffusion` on boundary exits, collisions, map violations, and official export zero-fallback checks.

## Blockers

- `nvidia-smi` still reports `GPU access blocked by the operating system`, but PyTorch CUDA is usable in this shell and Lightning training is running on the local RTX 4090. Use PyTorch/Lightning smoke checks rather than `nvidia-smi` alone to decide whether local training can run.
- Waymo official evaluation dependencies are not installed in the current environment, so official export assertion tests still skip here.

## Next Actions

- Run a real checkpoint layout export, for example `python scripts/render_multicamera_layout.py --config configs/validation/validation_scalable_hybrid_diffusion.yaml --ckpt checkpoints/hybrid_diffusion_1000/last.ckpt --index 0 --num-frames 16 --output-dir outputs/multicamera_layout_hybrid_idx0`. Add `--encode-video` only when per-camera MP4 clips are needed and `ffmpeg` is available; add `--draw-map-polylines` only when debugging projection geometry.
- Inspect `outputs/multicamera_layout_*/manifest.json` and several `frame_*/CAM_*.png` images before choosing the downstream video generator; the next implementation step should either add calibrated camera parameters or a thin adapter for the selected video model's conditioning format.
- Train the map-conditioned receding-horizon standalone ELF run with `python -u train.py --config configs/train/train_scalable_elf_3epoch_local.yaml --save_ckpt_path checkpoints/elf_receding_3epoch`, then regenerate visual checks from `checkpoints/elf_receding_3epoch/last.ckpt`.
- After receding ELF training, run `scripts/compare_motion_models.py` with `elf=configs/train/train_scalable_elf_1000.yaml=<new_receding_ckpt>` on the same validation indices used by `outputs/model_comparison_1000/`, then update the comparison summary to include ELF.
- Train the updated AR rerank server run with `python -u train.py --config configs/train/train_scalable_ar_diffusion_rerank.yaml --save_ckpt_path checkpoints/ar_rerank`, then compare straight-vehicle moving-speed ratios, late map adherence, ADE/FDE, and proposal-carry diagnostics against `ar_baseline`, `ar_frontier`, `causal_diffusion`, `causal_flow_matching`, and `hybrid_diffusion`; note that this run now uses causal temporal attention, `ar_training_mode: cadf_lite`, deterministic chunk0 anchor cycling, dense SMART CE replay every 8 steps, commit-primary chunk weights, terminal PAD supervision, carry-over proposal augmentation without explicit shift KL, no frontier loss, stricter contiguous-chain geometry confidence after masked chunk gaps, pure inference inputs, and stationary-token heading guards during retokenization.
- Retrain action-chunk with `configs/train/train_scalable_ar_action_chunk.yaml` on the server after the CE replay / shift-consistency change, then compare against the stale 1000-step checkpoint and AR rerank on the same fixed validation slice.
- Train continuous action diffusion with `configs/train/train_scalable_continuous_action_diffusion.yaml` on the server, then compare it against discrete ACT-style action-chunk, AR rerank, causal flow matching, and SMART on the same fixed validation slice. This is the current preferred branch if continuous geometry smoothness and temporal ensembling matter more than preserving a pure discrete-token ablation.
- Retry discrete diffusion policy with `16-mixed` using `configs/train/train_scalable_discrete_diffusion_policy.yaml` on the server, then compare it against AR rerank, discrete action-chunk, continuous action diffusion, causal flow matching, and SMART. Use the result to test whether discrete multi-token lookahead is useful when tail chunks are supervised as low-weight lookahead targets but are not carried, temporally ensembled, candidate-reranked, or committed.
- Retrain `configs/train/train_scalable_causal_flow_matching_1000.yaml` or a short local flow-matching run after the objective diagnostic fix, then regenerate validation visuals before judging flow quality.
- Use `outputs/model_comparison_1000/summary.csv` and per-model PNGs as the current local 8-scene comparison artifact; rerun `scripts/compare_motion_models.py` on a larger fixed validation slice before using the result as final evidence.
- Launch server flow-matching training with `configs/train/train_scalable_causal_flow_matching.yaml` after the GitHub PR is created. The first server config uses `flow_integration_steps: 1` to keep epoch-end rollout validation bounded; raise it to 2 or 4 for final quality sweeps.
- Run a short local flow-matching training job from `configs/train/train_scalable_causal_flow_matching_local.yaml` when debugging before server launch, then rerun the smoke and visual commands with the resulting checkpoint.
- Compare trained `smart_causal_flow_matching` against `smart_causal_diffusion` on the same demo and validation scenes using ADE/FDE, moving-speed ratios, `val_rollout_score`, guidance metrics, and whole-scene visualizations.
- Use `docs/aaai_causal_diffusion_method_zh.md` as the paper-style Chinese Method draft before writing the final AAAI English version.
- Use `docs/smart_causal_diffusion_overview.md` with `docs/train_scalable_causal_diffusion_config.md` as the current causal v2 architecture/config reference before editing or launching causal runs.
- Use `scripts/visualize_causal_guidance_modes.py --modes seed,none,safe,ego_stress,ego_edit --target-spec ego_risk --target-event-eta 0.0 --output-dir outputs/causal_ego_risk_guidance_visual` as the quick visual comparison command for generic ego-risk guidance.
- For final figures, first scan candidate scenes/agents by low ego TTC, small ego-target distance, route proximity, zero hard collision/offroad, and small non-target preservation error; only then render selected scenes.
- Run cut-in or lead-hard-brake target specs only as legacy ablations. For those runs, pass explicit target agents/windows after geometry scanning instead of relying on the default target selector.
- Run the five-epoch local demo command documented in `README.md` and confirm the model can overfit the 11 scenes before launching the full server run.
- Use `docs/train_scalable_causal_diffusion_config.md` as the field-by-field reference when editing the server causal config; verify all linked time/token fields remain consistent.
- Follow the `README.md` causal diffusion server checklist; the committed server config already uses epoch-based `warmup_steps: 2` and `total_steps: 32`.
- Archive `outputs/calibration/causal_retokenization_p99.json` with the run metadata; recalibrate only if the training dataset or perturbation policy changes.
- Start causal training with `configs/train/train_scalable_causal_diffusion.yaml`; checkpoint selection uses `val_rollout_score`, while `val_minADE`/`val_minFDE` remain baseline-comparison metrics.
- For AR diffusion finetunes initialized from older checkpoints, prefer `--pretrain_ckpt` over `--ckpt_path`; confirm LR is nonzero and GPU0 does not retain per-rank checkpoint-loading contexts after startup.
- For AR diffusion server debugging, inspect `[SMARTDiffusion]`, `[ValidationVisualization]`, and `[StepVisualization]` stdout logs to see whether slowdowns happen in window loss, 16-round receding-horizon rollout, per-round sampling, or rank0 visualization.
- For AR diffusion training, remember epoch-end validation is now bounded by `Trainer.limit_val_batches` in the AR train configs; remove or raise that field for full official validation.
- For AR diffusion, treat `val_ar_window_loss` as a short-window denoising diagnostic only; use rollout metrics such as `val_minADE`/`val_minFDE` for checkpoint selection.
- Keep `metric_mode: smart_val_compatible` for official SMART parity; use `metric_mode: smart_category3` only for target-only ablations.
- Use `source /home/anthony/anaconda3/etc/profile.d/conda.sh && conda activate smart` before running SMART tests in noninteractive shells.
- If straight-vehicle speed remains high after visible-token neighbor corruption, add a diagnostic that logs predicted and GT token speed percentiles by agent/category/current-speed bin.
